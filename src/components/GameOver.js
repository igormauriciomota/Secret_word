import './GameOver.css';

const GameOver = () => {
    return (
        <div className='end'>
            <h1>Secret Word</h1>
            <p>Clique no botão abaixo para começar a jogar</p>
            <button>Começar o jogo</button>
        </div>
    );
};

export default GameOver;